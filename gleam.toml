name = "counter"
version = "1.0.4"

description = "A counter for Gleam - modeled after Python's `collections.Counter`."
licences = ["Apache-2.0"]
repository = { type = "github", user = "JosephTLyons", repo = "counter" }

[dependencies]
gleam_stdlib = ">= 0.52.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.3.1 and < 2.0.0"
